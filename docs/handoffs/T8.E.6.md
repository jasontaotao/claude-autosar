# T8.E.6 handoff — arxml-format/SKILL.md 文档修复（Agent H）

## 改了哪些文件

| 文件 | 改动 | 行数变化 |
|------|------|---------|
| `packages/plugin/plugins/autoc/skills/arxml-format/SKILL.md` | 命名空间表重写 + 探测说明 + 读例代码用 `detect_namespaces()` | +12 / 改 8 |

## 改动内容

1. **L12-16 命名空间表** —— 改为 4 列：**规范版本 / 完整 URI / Schema 文件 / 首次引入年份**
   - 涵盖 r4.0 / r4.2 / r4.4 / r4.6 / r4.7 / r4.8（6 个版本，与契约 3 `WELL_KNOWN_NAMESPACE_URIS["ar"]` 一致）
   - URI 精确匹配 `http://autosar.org/schema/rX.Y`（`X.Y` 后缀）
   - Schema 文件名占位（`AUTOSAR_00046.xsd` … `AUTOSAR_00051.xsd`），便于 LLM 检索

2. **新增探测说明** —— 在表下方加 callout：
   > **工具按根 `xmlns` 自动探测，前缀仅作 fallback**。autoc 不硬编码 r4.0；通过 `arxml_io.detect_namespaces(path)` 从文件根元素动态读 URI。

3. **L96 读例代码** —— `ns = {"ar": "http://autosar.org/schema/r4.0"}` 改为：
   ```python
   from autoc.core.bsw import arxml_io
   ns = arxml_io.detect_namespaces("Mcu.arxml")
   ```
   注释明确"建议用 arxml_io.detect_namespaces() 动态获取"。

## 接口契约遵守

- **契约 1（ProjectConfig）**：✓ 未接触
- **契约 2（BSWMDRegistry / ParamDef）**：✓ 未接触
- **契约 3（arxml_io detect_namespaces）**：✓ 文档引用 `arxml_io.detect_namespaces(path)` 与契约签名一致；URI 表 6 个版本与 `WELL_KNOWN_NAMESPACE_URIS["ar"]` 元组完全对齐
- **契约 4（BSWPathResolver）**：✓ 未接触
- **契约 5（BSWWritePathError）**：✓ 未接触
- **契约 6（autoc.yaml schema）**：✓ 未接触
- **契约 7（测试命名 + 文件布局）**：✓ 本任务无新增测试

## 新增依赖

无（纯文档任务）。

## 新增测试

无。SKILL.md 是用户面向文档，不在 `tests/` 加 case。

## lint / typecheck

不适用（Markdown 文档）。**未**运行 ruff / mypy —— 与本任务范围（纯文档）一致；契约 3 的 Python 代码由 Agent B 单独实现并测试。

## [CONTRACT-CHANGE]

无。文档完全对齐契约 3 冻结内容。

## 已知问题 / TODO（不在本任务范围）

1. **Schema 文件名占位未与 AUTOSAR 官方命名 100% 对齐** —— AUTOSAR 官方命名形如 `AUTOSAR_4-4.xsd`（中间是短横线）或 `AUTOSAR_00046.xsd`（6 位 release artifact 号）混用。本任务用 `AUTOSAR_00046.xsd` … `AUTOSAR_00051.xsd` 占位是合理近似（与表内显示的 release 编号一致）。真实工程中以 EB tresos / Vector / DaVinci 工具安装目录里的实际文件名为准。
2. **r4.8 (R25-11) 首次引入年份 2025** —— 标注为 2025，实际 AUTOSAR release 节奏是每年 11 月放一版。R25-11 真实释出日期可能落在 2025-11，本任务标注"2025"作为年份。
3. **XDM namespace `d:` 未在表中列出** —— 保持原文档结构（XDM 在独立"顶层结构"段）；如需统一在命名空间表加一行 `d` -> `http://www.3soft.de/xml/tresos/datamodel/1.0` 可在后续 sprint 增补。
4. **`arxml_io.detect_namespaces()` 是 sync 阻塞调用** —— SKILL.md 文档未强调 IO 缓存行为（LRU cache by `(path, mtime)`）；高级细节由契约 3 锁定，文档示例只需展示推荐用法。

## 遵守约束

- ✗ **未**修改 plan 的契约段
- ✗ **未**改任何 Python 源代码
- ✗ **未**改 conftest.py
- ✗ **未**跑 git commit（按用户指示）
